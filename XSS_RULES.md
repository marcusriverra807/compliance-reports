## XSS Rules

- Rule 1: Always sanitize user input.
- Rule 2: Use HTML entity encoding for output.
- Rule 3: Implement a Content Security Policy (CSP).
- Rule 4: Avoid using `eval()` or similar functions.
- Rule 5: Validate and filter input on both client and server sides.